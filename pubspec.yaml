name: parentshield
description: ParentShield - Smart Parental Control App
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^2.27.0
  firebase_auth: ^4.17.0
  cloud_firestore: ^4.15.0
  firebase_messaging: ^14.7.0
  provider: ^6.1.1
  local_auth: ^2.1.8
  crypto: ^3.0.3
  flutter_secure_storage: ^9.0.0
  device_info_plus: ^9.1.1
  package_info_plus: ^5.0.1
  geolocator: ^11.0.0
  google_maps_flutter: ^2.5.3
  geocoding: ^2.1.1
  fl_chart: ^0.66.0
  qr_flutter: ^4.1.0
  intl: ^0.19.0
  shared_preferences: ^2.2.2
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  uuid: ^4.2.2
  connectivity_plus: ^5.0.2
  permission_handler: ^11.2.0
  flutter_local_notifications: ^17.0.0
  app_usage: ^2.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true
  assets:
    - assets/
